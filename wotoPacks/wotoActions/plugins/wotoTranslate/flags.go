package wotoTranslate

// the flags for the wotoTranslate plugin.
// please notice that all flags should begin with
// prefex --, but in the pTools, this prefex will be
// removed.
const (
	BIN_FLAG     = "bin"
	BINARY_FLAG  = "binary"
	PV_FLAG      = "pv"
	PRIVATE_FLAG = "private"
)
